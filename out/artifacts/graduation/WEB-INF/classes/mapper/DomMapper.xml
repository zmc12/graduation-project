<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.web.mapper.DomMapper">
    <resultMap id="BaseResultMap" type="com.jsut.web.pojo.Dom">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="number" jdbcType="VARCHAR" property="number"/>
        <result column="score" jdbcType="INTEGER" property="score"/>
        <result column="students" jdbcType="VARCHAR" property="students"/>
        <result column="fix" jdbcType="VARCHAR" property="fix"/>
        <result column="process" jdbcType="VARCHAR" property="process"/>
        <result column="grade" jdbcType="VARCHAR" property="grade"/>
        <result column="term" jdbcType="VARCHAR" property="term"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, number, score ,students,fix,process,grade,term
    </sql>

    <select id="select"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from dom where grade=#{grade} and term=#{term} order by id desc;
    </select>

    <select id="selectAll"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from dom order by id desc;
    </select>


    <insert id="insert" parameterType="com.jsut.web.pojo.Dom">
        insert into dom(number,score,students,fix,process,grade,term) values (#{number},#{score},#{students},#{fix},#{process},#{grade},#{term});
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from dom where id=#{id};
    </delete>

    <update id="updateById" parameterType="com.jsut.web.pojo.Dom">
        update  dom set number=#{number},score=#{score},students=#{students},fix=#{fix},process=#{process} where id=#{id};
    </update>


</mapper>