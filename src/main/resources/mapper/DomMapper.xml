<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.web.mapper.DomMapper">
    <resultMap id="BaseResultMap" type="com.jsut.web.pojo.Dom">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="number" jdbcType="VARCHAR" property="number"/>
        <result column="score" jdbcType="INTEGER" property="score"/>
        <result column="students" jdbcType="VARCHAR" property="students"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, number, score ,students
    </sql>

    <select id="selectAll"  resultMap="BaseResultMap">
        select  <include refid="Base_Column_List" /> from dom order by id desc;
    </select>


    <insert id="insert" parameterType="com.jsut.web.pojo.Dom">
        insert into dom(number,score,students) values (#{number},#{score},#{students});
    </insert>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from dom where id=#{id};
    </delete>

    <update id="updateById" parameterType="com.jsut.web.pojo.Dom">
        update  dom set number=#{number},score=#{score},students=#{students} where id=#{id};
    </update>


</mapper>