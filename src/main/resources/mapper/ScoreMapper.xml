<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsut.web.mapper.ScoreMapper">
    <resultMap id="BaseResultMap" type="com.jsut.web.pojo.Score">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="score" jdbcType="INTEGER" property="score"/>
        <result column="subject" jdbcType="VARCHAR" property="subject"/>
        <!--        <result column="college" jdbcType="VARCHAR" property="college"/>-->
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id,name, score, subject
    </sql>


    <select id="selectByName" parameterType="java.lang.String"  resultMap="BaseResultMap">
        select  * from score where name=#{name};
    </select>

    <update id="updateById" parameterType="com.jsut.web.pojo.Score">
        update  score set score=#{score} where name=#{name} and subject=#{subject};
    </update>

    <delete id="deleteById" >
        delete from score where id=#{id};
    </delete>

    <insert id="insert" parameterType="com.jsut.web.pojo.Score">
        insert into score (name,score,subject) values (#{name},#{score},#{subject});
    </insert>

    <select id="ifHas" parameterType="com.jsut.web.pojo.Score" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from score where name=#{name} and subject=#{subject};
    </select>

</mapper>